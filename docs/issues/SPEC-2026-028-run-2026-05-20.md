# SPEC-2026-028 — Ejecución de regresión (2026-05-20)

| Campo | Valor |
|-------|--------|
| **Ejecutor** | Agente (automático + checks AWS dev) |
| **Entorno** | Local Windows + AWS `asap_dev` / `us-east-1` |
| **Commit** | Working tree local (sin commit SPEC-027 pendiente) |
| **Veredicto global** | **NO APTO** para release — P0 agente + fixture + tests desactualizados |

---

## Resumen ejecutivo

| Capa | Resultado |
|------|-----------|
| Tests in-scope (trivia, onboarding, matches, KB, invitaciones, smoke) | **108 passed**, 1 skipped |
| Suite completa `pytest tests/unit tests/smoke` | **158 passed**, **45 failed**, 1 skipped |
| Infra AWS dev | Tabla + Lambdas + AgentCore **presentes** |
| Fixture `MATCH#` en DynamoDB | **0 partidos** — R-07 bloqueado |
| Invoke AgentCore directo | **FAIL** — init &gt; 30s |
| E2E Telegram manual (§5) | **No ejecutado** — requiere bot + usuario |

---

## 1. Tests automatizados

### 1.1 Suite alineada a features implementadas (§6 spec)

```text
PYTHONPATH=<repo>;<repo>/infrastructure/lambdas/telegram_webhook
py -m pytest tests/smoke/ tests/unit/test_trivia/ ... (ver spec §6)
→ 108 passed, 1 skipped in 5.9s
```

Incluye: smoke SPEC-027, trivia, onboarding, matches, KB resolve/web, invitaciones (servicio/comandos/auth), telegram stream_parse, bracket.

### 1.2 Suite completa (como CI con poetry — local sin poetry)

```text
→ 158 passed, 45 failed, 1 skipped
```

| Grupo fallos | Cant. | Causa | Acción |
|--------------|-------|-------|--------|
| `test_scoring_engine` | 35 | `NotImplementedError` en `scoring_engine.py` | **Fuera de alcance** §3 spec (R-17) — excluir de gate o implementar Sprint 2 |
| `test_tools_return_types` | 1 | Test espera 4 tools; agente tiene 7 (+ invitation, onboarding, trivia) | Actualizar test |
| `test_guardrails` | 2 | Texto `SYSTEM_PROMPT` cambió (`NO PODÉS` → redacción distinta) | Actualizar aserciones |
| `test_start_handler` | 4 | Handler retorna `tuple[str, markup]`; tests esperan `str` | Actualizar tests (regresión de API) |
| `test_kb_enrichment_service` | 2 | Mock `enqueue_enrichment` ya no existe en `kb_retrieval_tool` | Actualizar/eliminar tests |
| `test_user_dao_gsi` (u otro DDB) | 1 | Credenciales moto vs cliente real | Revisar mock en test |

**Nota entorno local:** Python 3.14 + `pip install` (sin Poetry). CI oficial usa Poetry 3.12.

---

## 2. Pre-requisitos infra (§4.1)

| Check | Estado | Detalle |
|-------|--------|---------|
| `ProdeTable-dev` | ✅ | ACTIVE, ~128 ítems |
| `prode-telegram-webhook-dev` | ✅ | Active, LastModified `2026-05-19T18:40:00Z` |
| `prode_mundial_dev` AgentCore | ✅ | READY, version **46**, updated `2026-05-19T18:39:39Z` |
| Lambdas KB | ✅ | ingest, query, enrichment presentes |
| Fixture `MATCH#` | ❌ | **Scan COUNT = 0** — ejecutar `scripts/ingest_matches.py` |
| Perfil AWS | ✅ | `asap_dev` → account `615216531593` |

---

## 3. Regresión P0 — Agente (R-00 / R-16)

### 3.1 Smoke SPEC-027 (local)

| Test | Resultado |
|------|-----------|
| `test_main_py_does_not_import_trivia_service` | ✅ |
| `test_main_imports_trivia_section_from_prompt_sections` | ✅ |
| `test_prompt_sections_loads_without_heavy_deps` | ✅ |
| `test_trivia_service_imports_jobs_when_present` | ✅ |

### 3.2 Invoke AgentCore (dev)

```text
invoke_agent_runtime → RuntimeClientError:
  "Runtime initialization time exceeded ... 30s"
```

**Veredicto 0.1 / 16.1:** ❌ — El runtime no responde a `hola` en tiempo; usuarios verían `Hubo un error...` si persiste.

### 3.3 CloudWatch `/aws/lambda/prode-telegram-webhook-dev`

Errores recientes (muestra):

- `Runtime initialization time exceeded` (19-may)
- `Received error (502) from runtime`
- Histórico: `ModuleNotFoundError: invitation_commands`, `No module named 'agent'` en web fallback (18-may) — posiblemente pre-deploy

**Acción:** Revisar logs del **runtime** AgentCore (no solo Lambda) tras deploy v46; confirmar ausencia de `ModuleNotFoundError: src.jobs`.

---

## 4. Matriz manual §5 — estado

| Área | P0 | Automatizado | Manual Telegram | Notas |
|------|-----|--------------|-----------------|-------|
| R-00 Agente | 0.1–0.3 | Parcial | ⏸ Pendiente | Init timeout en invoke directo |
| R-02 Auth | 2.1–2.2 | ✅ unit | ⏸ | |
| R-03 Invitaciones | 3.1–3.4 | ✅ parcial | ⏸ | Tests start_handler desactualizados |
| R-04/05 Onboarding | 4.1–4.4 | ✅ unit | ⏸ | |
| R-06 Comandos | 6.1–6.2 | — | ⏸ | |
| R-07 Fixture | 7.1–7.4 | ✅ unit | ⏸ | **Sin datos MATCH# en DDB** |
| R-08/09 KB+web | 8.1–8.4 | ✅ parcial | ⏸ | |
| R-10 Guardrails | 10.1–10.2 | ⚠️ tests fail | ⏸ | |
| R-11–15 Trivias | 11–13 | ✅ unit | ⏸ | |
| R-17 Scoring | — | ❌ stub | N/A | Fuera de alcance |

---

## 5. Criterios de salida §8

| Criterio | Cumple |
|----------|--------|
| 100 % P0 manual en verde | ❌ |
| pytest unit + smoke (CI) | ⚠️ 45 fails en suite completa; 108 OK en subconjunto |
| SPEC-027 desplegado | ⚠️ Runtime v46 reciente; init timeout impide validar |
| Sin regresión Alta sin mitigación | ❌ Agente + fixture |

---

## 6. Defectos abiertos (plantilla §9)

| Fecha | ID | Sev. | Síntoma | Mitigación |
|-------|-----|------|---------|------------|
| 2026-05-20 | REG-028-01 | **P0** | AgentCore init &gt; 30s / 502 | Revisar zip runtime, cold start, logs runtime |
| 2026-05-20 | REG-028-02 | **P0** | 0 partidos `MATCH#` en dev | `py scripts/ingest_matches.py` (o sync API) |
| 2026-05-20 | REG-028-03 | P2 | 45 tests fallan en suite completa | Actualizar tests obsoletos; excluir scoring del gate |
| 2026-05-20 | REG-028-04 | P2 | Local sin Poetry 3.12 | Usar `poetry install` para paridad CI |

---

## 7. Próximos pasos recomendados

1. **Ingestar fixture:** `py scripts/ingest_matches.py --env dev --profile asap_dev` (ajustar flags según script).
2. **Diagnosticar AgentCore:** logs runtime v46; rebuild `build-agent-zip.sh` si falta `src/jobs`; redeploy.
3. **Validación manual P0** en Telegram (admin `ae4e3b0d`): `hola`, KB, `/trivia`, `/invitar`.
4. **Arreglar tests:** `test_start_handler`, `test_tools_return_types`, guardrails, kb_enrichment.
5. Re-ejecutar esta spec tras deploy + ingest.

---

## 8. Comandos usados

```powershell
$env:PYTHONPATH="...\scalonia_agent;...\telegram_webhook"
py -m pytest tests/unit/ tests/smoke/ -q --no-cov

aws dynamodb describe-table --table-name ProdeTable-dev --profile asap_dev --region us-east-1
aws bedrock-agentcore-control list-agent-runtimes --profile asap_dev --region us-east-1
```
